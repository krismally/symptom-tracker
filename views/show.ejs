<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Symptom Log</title>
</head>
<body>
    <h1><%= log.date.toLocaleDateString("en-US", { weekday: "long", month: "short", day: "2-digit", year: "numeric" })%>'s Entry</h1>
    <ul>
        <li><%= log.dayRating %></li>
        <li><%= log.painLvl %></li>
        <li><%= log.fatigueLvl %></li>
        <li><%= log.cupsWater %></li>
        <li><%= log.sleepHrs %></li>
        <li><%= log.outsideTime %></li>
        <li><%= log.meds %></li>
        <li><%= log.mood %></li>
        <li><%= log.stretch %></li>
        <li>Food Log: </li>
        <ul>
            <% const meals = Object.entries(log.foodLog) %>
            <% meals.forEach(meal => { %>
                <% const mealType = meal[0] %>
                <% meal.reverse().pop() %>
            <li><%= `${mealType}: ${meal}` %></li>
            <% }) %>
        </ul>
        <li><%= log.dayNotes %></li>
    </ul>
</body>
</html>